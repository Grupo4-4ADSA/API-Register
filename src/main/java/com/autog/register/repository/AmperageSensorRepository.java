package com.autog.register.repository;

import com.autog.register.entity.AmperageSensor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AmperageSensorRepository extends JpaRepository<AmperageSensor, Integer> {
}
